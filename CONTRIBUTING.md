# Contributing

Keep duplicate detection changes deterministic: use stable ordering for paths
and report the comparison mode in diagnostics. Add a focused fixture for a new
edge case and run the full test command before opening a pull request.

Do not commit generated scan output or files containing personal data.
